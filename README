Install:
    pip install keyboard, tk, mouse
Run:
    python <name.py>
    OR
    yarn pro

Build EXE: 
    - pip install pyinstaller
    - yarn build-pro # It build to dist/PokeAuto.exe
PRO requirement:
    Require modules: keyboard
                     tk
                     mouse
